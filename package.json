{
  "name": "online-clipboard-monorepo",
  "private": true,
  "type": "module",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev:convex": "convex dev",
    "dev": "npm-run-all -p dev:convex frontend:dev backend:dev",
    "frontend:dev": "npm run dev --workspace frontend",
    "backend:dev": "npm run dev --workspace backend",
    "clean": "rimraf node_modules frontend/node_modules backend/node_modules frontend/node_modules/.vite || true",
    "reinstall": "npm run clean && npm install && npm install --workspace frontend && npm install --workspace backend",
    "rebuild:convex": "npx convex dev --once",
    "doctor": "npm run reinstall && npm run rebuild:convex"
  },
  "dependencies": {
    "nanoid": "^5.0.7"
  },
  "devDependencies": {
    "convex": "^1.25.4",
    "npm-run-all": "^4.1.5",
    "typescript": "^5.5.4",
    "@types/node": "^20.14.2",
    "rimraf": "^5.0.5"
  }
}
