{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "install-backend-deps",
			"type": "shell",
			"command": "powershell",
			"args": [
				"-Command",
				"cd backend; if(Test-Path node_modules){Write-Host 'backend deps already installed'} else { npm install }"
			]
		}
	]
}